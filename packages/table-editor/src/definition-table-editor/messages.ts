import {Messages} from './types';

const DefaultMessages: Messages = {
  formIsInflected: () => 'Form is inflected automatically.',
  formIsDeleted: () => 'Form is deleted.',
  customForm: () => 'Custom form.',
  cellDialogInputHelper: () => 'Type here to define a custom form',
  useDefaultFormButton: () => 'Revert to default form',
  useDefaultFormMenu: () => 'Use default form',
  deleteThisForm: () => 'Delete this form',
};

export default DefaultMessages;
