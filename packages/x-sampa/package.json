{
  "name": "@condict/x-sampa",
  "version": "0.1.0",
  "description": "Utility for converting X-SAMPA (ASCII-based phonetic notation) to IPA (international phonetic alphabet).",
  "repository": "github:arimah/condict",
  "author": "Alice Heurlin <alice.s.heurlin@gmail.com>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "clean": "rimraf dist",
    "compile": "tsc",
    "build": "npm run clean && npm run build:data && npm run build:main",
    "build:data": "node build",
    "build:main": "npm run compile && npm run copyData",
    "copyData": "copyfiles --up 1 src/data.json dist/",
    "prepublishOnly": "npm install && npm run clean && npm run build",
    "lint": "eslint --ext .ts,.tsx src/",
    "test": "mocha"
  },
  "devDependencies": {
    "@condict/eslint-plugin": "^0.2.0",
    "chalk": "^4.1.0",
    "copyfiles": "^2.3.0",
    "cross-env": "^7.0.2",
    "eslint": "^7.3.0",
    "mocha": "^8.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.5"
  }
}
