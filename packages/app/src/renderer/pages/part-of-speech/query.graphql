query($id: PartOfSpeechId!) {
  partOfSpeech(id: $id) {
    name

    language {
      id
      name
    }

    inflectionTables {
      id
      name
      timeCreated
      timeUpdated

      usedByDefinitions {
        page {
          totalCount
        }
      }
    }
  }
}
