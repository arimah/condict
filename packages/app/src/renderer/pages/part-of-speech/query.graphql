query($id: PartOfSpeechId!) {
  partOfSpeech(id: $id) {
    name

    language {
      id
      name
    }

    inflectionTables {
      id
      name
      timeCreated
      timeUpdated

      usedByDefinitions {
        page {
          totalCount
        }
      }
    }

    usedByDefinitions(page: {page: 0, perPage: 5}) {
      page {
        totalCount
      }
      nodes {
        id
        term

        description {
          ...RichTextBlockFragment
        }

        timeCreated
        timeUpdated
      }
    }
  }
}
