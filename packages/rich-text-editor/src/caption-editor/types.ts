import {InlineMessages} from '../types';

// This interface is deliberately left empty. It may not need anything extra,
// but having it here makes things easier to change down the line.
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Messages extends InlineMessages { }
