export default class IpaChar {
  constructor(name, input, display, group, terms) {
    this.name = name;
    this.input = input;
    this.display = display;
    this.group = group;
    this.terms = terms;
  }
}
