import React from 'react';
import Head from 'next/head';

const Page = (): JSX.Element => <>
  <Head>
    <title>Condict UI components</title>
  </Head>

  <p>Select a group above to try out its components &uarr;</p>
</>;

export default Page;
